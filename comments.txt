git init
git add .
git commit -m 'first'
gst
git remote add origin https://github.com/Jitgitbit/kotlin_exercise_four.git
git push -u origin master
gst
git add .
git commit -m 'second (includes .idea/vcs.xml)'
git push origin
gst

git add .
git commit -m 'version control up to Pie, and from v 15'
git push origin
gst

(don't forget to sync in Android Studio!)

git add .
git commit -m 'CreateToDoActivity made'
git push origin
gst

git add .
git commit -m 'new intent made'
git push origin
gst

git add .
git commit -m 'create to do design'
git push origin
gst

git add .
git commit -m 'title works'
git push origin
gst

git add .
git commit -m 'making sure only this app can access these preferences'
git push origin
gst

git add .
git commit -m 'saving data in the shared preferences'
git push origin
gst

git add .
git commit -m 'saving data in the shared preferences, now obligatory to use safe calls!'
git push origin
gst

git add .
git commit -m 'all you need to add in the code to revert to the previous activity! lol'
git push origin
gst

git add .
git commit -m 'Pulling Data From Shared Preferences (check it in Logcat of Android Studio)'
git push origin
gst

git add .
git commit -m 'Just a little trick to avoid typo'
git push origin
gst

git add .
git commit -m 'started on adapter'
git push origin
gst

git add .
git commit -m 'layout added'
git push origin
gst

git add .
git commit -m 'title added to adapter'
git push origin
gst

git add .
git commit -m 'recyclerviewAdapter identified'
git push origin
gst

git add .
git commit -m 'required members implemented'
git push origin
gst

git add .
git commit -m 'layoutManager and adapter identified for MainActivity'
git push origin
gst

git add .
git commit -m 'Surround with null check for safety'
git push origin
gst

git add .
git commit -m 'now todos can be completed!'
git push origin
gst

git add .
git commit -m 'the (hamburger)menu is now operational'
git push origin
gst

git add .
git commit -m 'changed the colors'
git push origin
gst

git add .
git commit -m 'changed the colors bis'
git push origin
gst

<div>Icons made by <a href="https://www.flaticon.com/authors/freepik" title="Freepik">Freepik</a> from <a href="https://www.flaticon.com/" title="Flaticon">www.flaticon.com</a></div>

This application is written in Kotlin for Android 15 - 29. It is a very basic but functional tool for your daily activities. Keep your life organized with this simple app.

Some icons and images here will seem out of place. They are, I haven't yet found an efficient way to produce images and icons myself. So for now, I'm just trying to fill in the demanding requirements for google play publishment by downloading the right type and size images online, regardless of the content. I'm already happy if at some point I finally find one that get's accepted. lol

git add .
git commit -m 'app icon added'
git push origin
gst

git add .
git commit -m 'generated signed apk'
git push origin
gst